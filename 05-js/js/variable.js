//기본 const 를 쓰고 안되면 let 변경
const username = "홍길동";
// = 은 오른쪽에서 왼쪽으로 읽어야함 홍길동을 유저네임에 저장했다
// js 에서 = 는 같다는 표시가 아님. 속에 들어가 있다

console.log(username);

const a = 5;
const font1 = "돋움";
// 문자는 쌍따옴표를 한다
const _price = 30000;
const maxWidth = 600;
const max_width = 800;

console.log(a);
// f12 눌러서 콘솔누를 때 글자 나오게 하는 거
// 괄호 안에 a를 넣었으면 콘스탄트(const)위에 적은 숫자 5가 보인다
console.log(font1);
